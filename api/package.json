{
  "name": "collision-iq-api",
  "version": "1.0.0",
  "description": "API server for Collision IQ chatbot",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "nodemon server.mjs",
    "lint": "echo \"No linter configured\" && exit 0",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "keywords": ["Collision", "chatbot", "collision-iq"],
  "license": "MIT",
  "engines": { "node": ">=18" },
  "dependencies": {
    "@google/generative-ai": "^0.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.19.2",
    "googleapis": "^126.0.0",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}