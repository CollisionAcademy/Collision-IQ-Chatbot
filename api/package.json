{
  "name": "collision-iq-api",
  "version": "1.0.0",
  "description": "API server for Collision IQ chatbot",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "echo \"No linter configured\" && exit 0",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "keywords": ["collision", "chatbot", "api", "collision-iq"],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "@google/generative-ai": "^0.21.0",
    "dotenv": "^16.3.1",
    "googleapis": "^160.0.0",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}